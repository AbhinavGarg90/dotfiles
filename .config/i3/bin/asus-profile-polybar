#!/bin/sh

ACTIVE=$(asusctl profile get 2>/dev/null \
  | sed -n 's/^Active profile: //p' \
  | tr '[:upper:]' '[:lower:]')

case "$ACTIVE" in
  quiet)
    echo "󰌪 Quiet"
    ;;
  balanced)
    echo "󰗑 Balanced"
    ;;
  performance)
    echo "󰑣 Perf"
    ;;
  *)
    echo "󰍛 N/A"
    ;;
esac
